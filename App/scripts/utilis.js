// These function check and normalize data after the data is submitted
// We also assume the data is required by doing a check before calling 
// All the utility functions

const dataValidation = (...data) => {

};

const checkType = (data) => {
    data.type === 'string' ? "dosomething" : "dosomethingElse";
    data.type === 'number' ?;
    data.type === 'date' ? ;
    data.type === 'boolean' ?;
};

const formatString = (data);

const formatNumber;

const formatDate;

const formatBoolean;